{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Job{% endblock %}

{% block content %}
    <h1>Job</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ job.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_job_index') }}">back to list</a>

    <a href="{{ path('backend_job_edit', {'id': job.id}) }}">edit</a>

    {{ include('backend/job/_delete_form.html.twig') }}
{% endblock %}
