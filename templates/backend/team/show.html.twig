{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Team{% endblock %}

{% block content %}
    <h1>Team</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ team.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_team_index') }}">back to list</a>

    <a href="{{ path('backend_team_edit', {'id': team.id}) }}">edit</a>

    {{ include('backend/team/_delete_form.html.twig') }}
{% endblock %}
