{% extends 'backend/backend_layout.html.twig' %}

{% block title %}Team index{% endblock %}

{% block content %}
    <h1>Team index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team in teams %}
            <tr>
                <td>{{ team.id }}</td>
                <td>
                    <a href="{{ path('backend_team_show', {'id': team.id}) }}">show</a>
                    <a href="{{ path('backend_team_edit', {'id': team.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('backend_team_new') }}">Create new</a>
{% endblock %}
